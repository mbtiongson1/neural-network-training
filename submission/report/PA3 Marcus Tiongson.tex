\documentclass{acm_proc_article-sp}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\graphicspath{{../figures/}{submission/figures/}{figures/}}
\usepackage{url}
\usepackage{microtype}
\makeatletter
\providecommand{\@copyrightspace}{}
\makeatother

\begin{document}

\title{\large AI201 PA 3 Report\\[0.4em]
       \LARGE Implementing the Backpropagation Algorithm}

\numberofauthors{1}
\author{
  \alignauthor
  Marcus Rafael B. Tiongson (2013-59474)\\
  \affaddr{Date of Submission: March 27, 2026}
}

\maketitle

% -----------------------------------------------------------------------
\section{Introduction}
% -----------------------------------------------------------------------

This report documents a from-scratch NumPy implementation of the
backpropagation algorithm~\cite{rumelhart1986} applied to an eight-class
classification problem.  The dataset consists of 3\,486 instances with
354 numerical features per instance and a heavily skewed label
distribution: Class~1 accounts for 46.6\% of samples, making
straightforward gradient descent on the raw corpus prone to majority-class
collapse.  SMOTE~\cite{chawla2002,brownlee2021} is therefore applied
prior to training to enforce uniform class frequency.

Two MLP configurations are evaluated under identical mini-batch training
conditions, differing only in their hidden-layer activation functions:
Hyperbolic Tangent (Network~A) and Leaky ReLU (Network~B).  Both are
paired with a Logistic output layer and trained with the generalised delta
rule with momentum.  The core experimental question is how sensitive
convergence and final classification performance are to the choice of
learning rate $\eta$, momentum coefficient $\alpha$, and hidden-layer
width $H$, given a fixed two-hidden-layer architecture and a 100-epoch
budget.

% -----------------------------------------------------------------------
\section{Objectives}
% -----------------------------------------------------------------------

The primary objectives of this report are:

\begin{enumerate}
  \item To implement the backpropagation learning algorithm for a
        fully-connected MLP with two hidden layers and one output layer,
        using only the NumPy, Matplotlib, OS, and CSV libraries.
  \item To address severe class imbalance in the training set via SMOTE
        oversampling before model training.
  \item To compare two activation-function regimes (Hyperbolic Tangent
        and Leaky Rectified Linear Unit) across their respective hidden
        layers, using the Logistic function at the output layer for all
        configurations.
  \item To tune the key hyperparameters (learning rate $\eta$, momentum
        coefficient $\alpha$, and hidden-layer width) so that the networks
        converge to a low mean square error and a high macro-averaged F1
        score on the validation set.
  \item To generate and export class predictions for the unlabelled test
        set using the best-performing trained model.
\end{enumerate}

% -----------------------------------------------------------------------
\section{Methodology}
% -----------------------------------------------------------------------

\subsection{Dataset Loading}

The dataset is stored in three comma-separated files.  \texttt{data.csv}
contains 3\,486 instances, each with 354 numerical features.
\texttt{data\_labels.csv} holds the corresponding integer class labels
$y \in \{1, 2, \ldots, 8\}$.  \texttt{test\_set.csv} contains an
unlabelled test partition for which predictions are ultimately exported.
After loading, each label $y_i$ is one-hot encoded into an
$(N \times 8)$ matrix $\mathbf{D}$, where row $i$ has a $1.0$ at
position $y_i - 1$ and $0.0$ elsewhere:

\begin{equation}
  D_{ik} =
  \begin{cases}
    1.0 & \text{if } k = y_i - 1 \\
    0.0 & \text{otherwise}
  \end{cases}
\end{equation}

\subsection{Class Imbalance and SMOTE}

The original dataset exhibits pronounced class imbalance, with Class~1
representing 46.6\% of samples and Class~4 holding the smallest share.
Training an MLP directly on such skewed data risks the network learning a
trivial solution that always predicts the majority class, thereby
achieving superficially high accuracy while entirely ignoring minority
classes.

To mitigate this, SMOTE~\cite{chawla2002,brownlee2021} is applied.
SMOTE generates synthetic minority-class samples by interpolating between
a minority instance and one of its $k$-nearest neighbours in feature
space.  For each minority-class instance $\mathbf{x}_i$, a neighbour
$\mathbf{x}_{nn}$ is chosen at random and a synthetic point
$\mathbf{x}_{syn}$ is created as:

\begin{equation}
  \mathbf{x}_{syn} = \mathbf{x}_i + \lambda \,
  (\mathbf{x}_{nn} - \mathbf{x}_i), \quad \lambda \sim \mathcal{U}(0,1)
\end{equation}

After applying SMOTE with \texttt{random\_state=50}, every class holds an
equal number of samples, yielding a balanced training corpus.

\textit{Note on data leakage:} SMOTE was applied to the full labelled
dataset prior to the train/validation split.  Synthetic samples could
therefore appear in both partitions, potentially leading to optimistic
validation metrics.  Ideally, SMOTE should be applied only within the
training fold after partitioning; this trade-off was accepted for
implementation simplicity.

Figure~\ref{fig:piecharts} shows the class distribution before and after
SMOTE.

\begin{figure*}[ht]
\centering
\begin{minipage}{0.48\textwidth}
  \centering
  \includegraphics[width=\textwidth]{class_distribution:_original_dataset.png}
\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}
  \centering
  \includegraphics[width=\textwidth]{class_distribution_after_smote.png}
\end{minipage}
\caption{Class distribution of the original dataset (left) and after SMOTE
oversampling (right).  Class~1 accounts for 46.6\% of the 3\,486 original
instances and Class~4 is the smallest minority.  SMOTE raises all minority
classes to 1\,625 samples each, producing a perfectly uniform distribution
of 13\,000 instances total.}
\label{fig:piecharts}
\end{figure*}

\subsection{Dataset Partitioning}

The balanced dataset is split into a \textbf{training set} and a
\textbf{validation set}.  Exactly 800 samples are drawn uniformly at
random (without replacement, \texttt{random\_state=50}) as the validation
set; all remaining samples form the training set.  Both partitions are
exported to CSV for reproducibility.

\subsection{Network Architecture}

The MLP consists of three learnable layers:
\begin{itemize}
  \item \textbf{Hidden layer $i$:} receives a bias-augmented input vector
        and maps it to a vector of size $H$.
  \item \textbf{Hidden layer $j$:} receives the bias-augmented output of
        layer $i$ and maps it to a vector of size $H$.
  \item \textbf{Output layer $k$:} receives the bias-augmented output of
        layer $j$ and produces 8 output activations, one per class.
\end{itemize}

Bias units are handled implicitly: a constant $\varphi_0 = +1$ is
prepended to every layer's input vector, so the corresponding weight
$w^{(l)}_{j0}$ acts as the bias for neuron $j$ in layer $l$.  Weight
matrices are initialised from a zero-mean Gaussian with standard
deviation 0.01.

\subsection{Forward Propagation}

For layer $l \in \{i, j, k\}$, the \textit{internal activity} (net
input) of neuron $j$ is:

\begin{equation}
  v^{(l)}_j(n) = \sum_{m=0}^{p} w^{(l)}_{jm}(n)\,
  \varphi_m\!\left(v^{(l-1)}_m(n)\right)
  \label{eq:internal}
\end{equation}

where $p$ is the width of layer $l-1$, the index $m=0$ corresponds to
the bias unit ($\varphi_0 = +1$), and $n$ denotes the current training
step.  The \textit{neuron output} is then:

\begin{equation}
  y^{(l)}_j(n) = \varphi_j\!\left(v^{(l)}_j(n)\right)
  \label{eq:output}
\end{equation}

\subsection{Activation Functions}

Three activation functions are studied.  Derivatives are expressed
directly in terms of the layer output $o = \varphi(v)$ to avoid
recomputing $v$ during backpropagation.

\subsubsection{Logistic (Sigmoid)}

\begin{equation}
  \varphi(v) = \frac{1}{1 + e^{-av}}, \quad a > 0
  \label{eq:logistic}
\end{equation}
\begin{equation}
  \varphi'(v) = a \cdot o\,(1 - o)
\end{equation}

The parameter $a = 2.0$ is used at the output layer for all network
configurations.  Larger $a$ steepens the sigmoid.

\subsubsection{Hyperbolic Tangent (Tanh)}

\begin{equation}
  \varphi(v) = a \tanh(bv) =
  a\cdot\frac{e^{bv} - e^{-bv}}{e^{bv} + e^{-bv}}
  \label{eq:tanh}
\end{equation}
\begin{equation}
  \varphi'(v) = \frac{b}{a}\,(a - o)\,(a + o)
\end{equation}

Following standard practice~\cite{lecun1998}, the constants
$a = 1.716$ and $b = 0.66667$ are used for the hidden layers of
Network~A.

\subsubsection{Leaky ReLU}

\begin{equation}
  \varphi(v) =
  \begin{cases}
    v         & \text{if } v > 0 \\
    \gamma v  & \text{if } v \leq 0
  \end{cases}, \quad \gamma \in (0,1)
  \label{eq:lrelu}
\end{equation}
\begin{equation}
  \varphi'(v) =
  \begin{cases}
    1       & \text{if } o > 0 \\
    \gamma  & \text{if } o \leq 0
  \end{cases}
\end{equation}

With $\gamma = 0.01$, negative inputs receive a small but non-zero
gradient, preventing the ``dying ReLU'' problem.  Gradient clipping
(norm threshold 5.0) is applied to Leaky ReLU hidden layers to guard
against exploding gradients.

\subsection{Error Computation}

The \textit{error signal} at output neuron $k$ is the difference between
the desired output $d_k$ and the actual output $o_k$:

\begin{equation}
  e_k(n) = d_k(n) - o_k(n)
\end{equation}

The \textit{mean square error} (MSE) over all $C$ output neurons at step
$n$ is:

\begin{equation}
  \mathcal{E}(n) = \frac{1}{2}\sum_{k=1}^{C} e_k^2(n)
  \label{eq:mse}
\end{equation}

\subsection{Backpropagation Delta Equations}

\subsubsection{Output Layer Delta}

The local gradient (delta) for output neuron $k$ is:

\begin{equation}
  \delta^{(L)}_k(n) = e_k(n) \cdot \varphi'\!\left(v^{(L)}_k(n)\right)
  \label{eq:delta_out}
\end{equation}

\subsubsection{Hidden Layer Delta}

For hidden layer $l \in \{L-1, L-2, \ldots, 1\}$:

\begin{equation}
  \delta^{(l)}_j(n) =
  \varphi'\!\left(v^{(l)}_j(n)\right)
  \cdot \sum_{k}\, \delta^{(l+1)}_k(n)\, w^{(l+1)}_{kj}(n)
  \label{eq:delta_hidden}
\end{equation}

The summation propagates the error signal backward from layer $l+1$,
weighted by the connections $w^{(l+1)}_{kj}$.  Bias weights (column 0)
are excluded from this sum because the bias neuron carries no incoming
error.

\subsection{Weight Update: Generalised Delta Rule with Momentum}

The weight correction for connection $w^{(l)}_{ji}$ follows the
\textit{negative gradient} of $\mathcal{E}$ with respect to that weight:

\begin{equation}
  -\frac{\partial\mathcal{E}(n)}{\partial w^{(l)}_{ji}(n)}
  = \delta^{(l)}_j(n) \cdot \varphi_i\!\left(v^{(l-1)}_i(n)\right)
\end{equation}

The full update rule adds a momentum term that carries forward a
fraction $\alpha$ of the previous weight increment:

\begin{equation}
  \Delta w^{(l)}_{ji}(n)
  = \alpha\,\Delta w^{(l)}_{ji}(n-1)
  + \eta\,\delta^{(l)}_j(n)\,\varphi_i\!\left(v^{(l-1)}_i(n)\right)
  \label{eq:dw}
\end{equation}

\begin{equation}
  w^{(l)}_{ji}(n+1) = w^{(l)}_{ji}(n) + \Delta w^{(l)}_{ji}(n)
  \label{eq:w_update}
\end{equation}

Bias weights ($i=0$, with $\varphi_0 = +1$) are updated identically:

\begin{equation}
  \Delta w^{(l)}_{j0}(n)
  = \alpha\,\Delta w^{(l)}_{j0}(n-1)
  + \eta\,\delta^{(l)}_j(n)
\end{equation}

When $\alpha = 0$ the rule reduces to vanilla gradient descent.  When
$\alpha > 0$ it smooths oscillations in the weight space and can
accelerate convergence along consistent gradient directions.

\subsection{Mini-Batch Gradient Descent}

Rather than updating weights after every sample (online) or after the
full dataset (batch), a \textit{mini-batch} strategy with batch size
$B = 8$ is used.  Gradients are accumulated across all samples in a
batch and then the average gradient is applied:

\begin{equation}
  \Delta w^{(l)}_{ji}(n)
  = \alpha\,\Delta w^{(l)}_{ji}(n-1)
  + \frac{\eta}{B}\sum_{s=1}^{B}
    \delta^{(l)}_{j,s}(n)\,\varphi_{i,s}\!\left(v^{(l-1)}_i(n)\right)
\end{equation}

Mini-batch training reduces gradient variance compared to online updates
and is more computationally efficient than full-batch gradient descent.

\subsection{Validation and Evaluation Metrics}

After each epoch the model is evaluated on the 800-sample validation
set.  Predictions are determined by the $\arg\max$ of the output
layer activations.  The following metrics are computed from the
resulting $8\times 8$ confusion matrix:

\begin{itemize}
  \item \textbf{Accuracy} $= \dfrac{\text{TP}_\text{total}}{N}$
  \item \textbf{Macro-averaged Precision, Recall, F1} computed
        per class and then averaged uniformly across all eight classes.
  \item \textbf{Matthews Correlation Coefficient (MCC)} for
        multiclass classification, defined as:
        \begin{equation}
          \text{MCC} =
          \frac{c\,s - \textstyle\sum_k p_k t_k}
               {\sqrt{\left(s^2 - \textstyle\sum_k t_k^2\right)
                      \left(s^2 - \textstyle\sum_k p_k^2\right)}}
        \end{equation}
        where $s$ is the total number of samples, $c$ is the number
        correctly classified, $t_k$ is the actual count of class $k$,
        and $p_k$ is the predicted count of class $k$.
\end{itemize}

% -----------------------------------------------------------------------
\section{Experimental Results}
% -----------------------------------------------------------------------

\subsection{Hyperparameter Configurations}

Two base network architectures are evaluated: \textbf{Network A} uses
Tanh activation ($a = 1.716,\; b = 0.66667$) in both hidden layers, and
\textbf{Network B} uses Leaky ReLU ($\gamma = 0.01$) in both hidden
layers.  Both networks use a Logistic output layer ($a = 1.0$).  Each
family is evaluated under five settings: the default configuration, a
first stable run at $\eta=0.10$ and $\alpha=0.50$, a second run with a
lower learning rate $\eta=0.05$, a high-momentum ``fast'' variant with $\alpha=0.90$, and a
reduced-width $H=5$ variant.

\begin{table*}[ht]
\centering
\small
\caption{Hyperparameter Configurations Tested}
\label{tab:configs}
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{lcccc}
\toprule
Configuration & $\eta$ & $\alpha$ & $H$ & Activation \\
\midrule
\texttt{networkA}                 & 0.85 & 0.90 & 8  & Tanh \\
\texttt{networkA\_improv}         & 0.10 & 0.50 & 12 & Tanh \\
\texttt{networkA\_improv2}        & 0.05 & 0.50 & 12 & Tanh \\
\texttt{networkA\_improv2\_fast}  & 0.05 & 0.90 & 12 & Tanh \\
\texttt{networkA\_improv2\_small} & 0.05 & 0.90 & 5  & Tanh \\
\texttt{networkB}                 & 0.85 & 0.90 & 8  & Leaky ReLU \\
\texttt{networkB\_improv}         & 0.10 & 0.50 & 12 & Leaky ReLU \\
\texttt{networkB\_improv2}        & 0.05 & 0.50 & 12 & Leaky ReLU \\
\texttt{networkB\_improv2\_fast}  & 0.05 & 0.90 & 12 & Leaky ReLU \\
\texttt{networkB\_improv2\_small} & 0.05 & 0.90 & 5  & Leaky ReLU \\
\bottomrule
\end{tabular}
\end{table*}

All configurations are trained for 100 epochs using mini-batch size
$B = 8$.  After SMOTE and the fixed 800-sample validation split, the
training set contains 12\,200 samples, so each run performs 1\,525
mini-batch updates per epoch, or 152\,500 updates over 100 epochs.

\subsection{Training and Validation Errors}

The two default runs never entered a useful learning regime.  For
\texttt{networkA}, the training/validation MSE moved from
$(0.808,\;0.867)$ at epoch~1 to a plateau near $(0.875,\;0.867)$ and
never improved thereafter.  \texttt{networkB} behaved slightly
differently: it briefly reached minima of approximately
$(0.401,\;0.394)$, but then drifted upward and finished near
$(0.874,\;0.889)$.  In both cases, the models failed to satisfy the
notebook's convergence criterion that both training and validation MSE
drop below 0.02 within the 100-epoch budget.

Lowering the learning rate to 0.10 or 0.05 was what made learning
possible, but the two activation families still behaved differently.
The tuned Tanh models converged more smoothly, and all four non-default
variants eventually met the 0.02 criterion.  The earliest was
\texttt{networkA\_improv2\_fast}, which crossed the threshold at
epoch~44.  The tuned Leaky ReLU models achieved the highest final
classification scores, but their validation curves were visibly more
erratic.  The best overall
model, \texttt{networkB\_improv2}, reached the threshold only at
epoch~73, while \texttt{networkB\_improv2\_fast} shortened this to
epoch~51 at a small cost in F1 score.  The reduced-width Leaky ReLU
model, \texttt{networkB\_improv2\_small}, remained unstable and never
converged.

\subsection{Scores Summary}

Table~\ref{tab:scores} reports the final evaluation metrics on the
800-sample validation set for all ten configurations, ranked by the same
macro-F1 criterion used in the notebook's \textit{Training Results}
section.

\begin{table*}[ht]
\centering
\small
\caption{Validation-set summary ranked by macro F1.  ``Epoch'' denotes
the first epoch at which both training and validation MSE fell below
0.02; DNF indicates that this did not occur within 100 epochs.
Time-adjusted cost is computed as $\text{Time}\times(\text{Epoch}/100)$
for converged runs.}
\label{tab:scores}
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{lccccc}
\toprule
Configuration & Epoch & F1 & MCC & Acc. & Time-adj. (s) \\
\midrule
\texttt{networkB\_improv2}        & 73  & 0.98861 & 0.98714 & 0.98875 & 70.75 \\
\texttt{networkB\_improv2\_fast}  & 51  & 0.98530 & 0.98286 & 0.98500 & 44.58 \\
\texttt{networkA\_improv2\_fast}  & 44  & 0.98352 & 0.98011 & 0.98250 & 29.70 \\
\texttt{networkA\_improv2\_small} & 74  & 0.98108 & 0.97860 & 0.98125 & 31.34 \\
\texttt{networkA\_improv}         & 56  & 0.98006 & 0.97725 & 0.98000 & 29.26 \\
\texttt{networkA\_improv2}        & 74  & 0.97609 & 0.97309 & 0.97625 & 43.91 \\
\texttt{networkB\_improv}         & 71  & 0.96129 & 0.95481 & 0.96000 & 52.80 \\
\texttt{networkB\_improv2\_small} & DNF & 0.55645 & 0.59344 & 0.61250 & N/A   \\
\texttt{networkA}                 & DNF & 0.02925 & 0.00000 & 0.13250 & N/A   \\
\texttt{networkB}                 & DNF & 0.02503 & 0.00000 & 0.11125 & N/A   \\
\bottomrule
\end{tabular}
\end{table*}

The ranking separates the tuned models from the failed ones very
clearly.  Seven configurations achieved macro F1 above 0.96, three of
them above 0.98, whereas the two defaults and the reduced-width Leaky
ReLU run fell far outside that cluster.  If convergence speed is also
considered, \texttt{networkA\_improv2\_fast} offers the strongest
efficiency--accuracy compromise: it reaches the target error in 44
epochs and retains a macro F1 of 0.9835.  Although
\texttt{networkA\_improv} has a slightly smaller time-adjusted cost
(29.26\,s versus 29.70\,s), the higher-momentum Tanh model gives a
clearer gain in predictive quality for nearly the same normalized
training expense.

\begin{figure*}[ht]
\centering
\begin{minipage}{0.49\textwidth}
  \centering
  \includegraphics[width=\textwidth]{learning_curves_of_network_a_compared.png}
\end{minipage}
\hfill
\begin{minipage}{0.49\textwidth}
  \centering
  \includegraphics[width=\textwidth]{learning_curves_of_network_b_compared.png}
\end{minipage}
\caption{Training and validation MSE for the five Tanh configurations
(left) and five Leaky ReLU configurations (right).  Lowering the
learning rate enables convergence in both families, higher momentum
compresses the number of epochs needed once the learning rate is
stabilized, and the 5-node Leaky ReLU model is the clearest failure
case.}
\label{fig:learningcurve}
\end{figure*}

\subsection{Best Model and Prediction}

\texttt{networkB\_improv2} is the best overall model.  It uses Leaky
ReLU with $\eta=0.05$, $\alpha=0.50$, and $H=12$, and achieves
98.875\% accuracy, MCC $=0.98714$, and macro F1 $=0.98861$ on the
validation set.  Within the Tanh family, the selected model is
\texttt{networkA\_improv2\_fast}, which uses $\eta=0.05$,
$\alpha=0.90$, and $H=12$; it converges much earlier at epoch~44 while
still reaching macro F1 $=0.98352$.

These are also the two models exported, ready for production.  The Tanh
weights are saved to \texttt{modelA/trained\_weights.csv}, the Leaky
ReLU weights to \texttt{modelB/trained\_weights.csv}, and their
respective 701-sample test-set predictions are written to
\texttt{predictions/predictions\_for\_test\_tanh.csv} and
\texttt{predictions/predictions\_for\_test\_leakyrelu.csv}.  The
distinction is important: the Leaky ReLU model is the best by absolute
validation score, whereas the Tanh model is retained because it gives
the strongest speed--performance compromise within its activation
family.

% -----------------------------------------------------------------------
\section{Analysis and Discussion of Results}
% -----------------------------------------------------------------------

\subsection{Impact of Learning Rate}

The single most consequential hyperparameter change was reducing the
learning rate from $\eta = 0.85$ to $\eta = 0.10$.  At 0.85, the weight
increments $\Delta w^{(l)}_{ji}$ (Equation~\ref{eq:dw}) were so large
that the optimiser repeatedly jumped over the minimum of
$\mathcal{E}$, producing plateaued or unstable error curves and
near-zero MCC scores.
Both the Tanh and Leaky ReLU networks were equally vulnerable to this
instability, underscoring that activation function choice is secondary to
choosing an appropriate step size.

\subsection{Role of Momentum}

The notebook results show that momentum only became useful after the
learning rate had already been reduced to a stable range.  Holding
$\eta=0.05$ and $H=12$ fixed, increasing $\alpha$ from 0.50 to 0.90
reduced the convergence epoch for Tanh from 74
(\texttt{networkA\_improv2}) to 44
(\texttt{networkA\_improv2\_fast}) and simultaneously improved macro F1
from 0.97609 to 0.98352.  For Leaky ReLU, the same increase reduced the
convergence epoch from 73 (\texttt{networkB\_improv2}) to 51
(\texttt{networkB\_improv2\_fast}), but at a slight cost in macro F1
(0.98861 down to 0.98530).

Momentum acts like an
``epoch-skipping'' term: it compresses the loss curve horizontally and
is best viewed as a convergence-speed control rather than a guaranteed
way to improve the final optimum.  High momentum was not enough to save
the default $\eta=0.85$ runs, so it should be tuned only after the step
size is already well behaved.

\subsection{Hidden Layer Width}

The width experiments in the notebook compare $H=12$ against a reduced
$H=5$ model, not against an intermediate width.  At $\eta=0.05$ and
$\alpha=0.90$, shrinking the Tanh network from 12 to 5 hidden units
reduced raw training time from 67.50\,s to 42.36\,s, but also delayed
convergence from epoch~44 to epoch~74 and lowered macro F1 from 0.98352
to 0.98108.  The smaller Tanh model therefore remains viable only when
compute is constrained.

The same reduction was far more damaging for Leaky ReLU.
\texttt{networkB\_improv2\_small} never achieved the 0.02 MSE target and
collapsed to macro F1 $=0.55645$, far below the 0.98861 attained by the
12-node \texttt{networkB\_improv2}.  Hidden-layer width therefore
interacts strongly with activation choice: $H=12$ is the safer default,
while $H=5$ is defensible only for the Tanh family and only as a
hardware-driven compromise.

\subsection{Activation Function Comparison}

Leaky ReLU produced the strongest peak validation scores, occupying the
top two positions in Table~\ref{tab:scores} with
\texttt{networkB\_improv2} and \texttt{networkB\_improv2\_fast}.  The
best Tanh model, \texttt{networkA\_improv2\_fast}, ranked third overall
with macro F1 $=0.98352$, roughly 0.51 percentage points below the best
Leaky ReLU result.  If the comparison stopped at peak F1, Leaky ReLU
would be the clear winner.

The learning-curve evidence adds a more nuanced interpretation.  The
notebook commentary correctly notes that Leaky ReLU exhibited more
erratic validation behavior than Tanh, and the small-width ReLU variant
failed completely.  Tanh converged more smoothly, tolerated the reduced
$H=5$ setting, and delivered a much better efficiency--accuracy tradeoff
in \texttt{networkA\_improv2\_fast}.  The practical conclusion is
therefore twofold: Leaky ReLU is the better choice when absolute
predictive performance is the priority, while Tanh is the more robust
choice when convergence smoothness and computational efficiency also
matter.

\subsection{Effect of SMOTE on Class Balance}

Applying SMOTE prior to training yielded a perfectly uniform class
distribution, which directly enabled the network to learn discriminative
features for all eight classes.  Without class balancing, Class~1
(46.6\% of data) would dominate the gradient signal, causing the network
to converge to a trivial majority-class predictor, consistent with
the near-zero MCC observed under default hyperparameters before
balancing.  However, the leakage introduced by balancing before
splitting means the 98.6\% validation accuracy is likely slightly
optimistic compared to what would be observed on truly unseen data.

\subsection{Convergence Rate and Early Stopping}

The fast variants captured most of their usable gains early:
\texttt{networkA\_improv2\_fast} met the target at epoch~44 and
\texttt{networkB\_improv2\_fast} at epoch~51.  The highest-F1 models,
however, required longer runs: \texttt{networkB\_improv2} did not cross
the threshold until epoch~73, and several Tanh variants also converged
only after epoch~56.  Early stopping is therefore still sensible, but
its patience should be tuned per configuration rather than fixed at a
single aggressive cutoff such as epoch~40.

% -----------------------------------------------------------------------
\section{Conclusion}
% -----------------------------------------------------------------------

This work demonstrated a complete, from-scratch implementation of the
backpropagation algorithm on an eight-class imbalanced dataset.  The
following conclusions are drawn:

\begin{enumerate}
  \item \textbf{The default configuration was not usable.} The baseline
        \texttt{networkA} and \texttt{networkB} runs both failed to
        converge and finished with macro F1 scores of only 0.02925 and
        0.02503, respectively.  The original $\eta=0.85$ and
        $\alpha=0.90$ setting was simply too aggressive for this task.

  \item \textbf{Lowering the learning rate was the main factor that made
        training succeed.} Once $\eta$ was reduced to 0.10 or 0.05, both
        activation families produced strong models, and the best overall
        result came from \texttt{networkB\_improv2} with macro
        F1 $=0.98861$.

  \item \textbf{Higher momentum improved convergence speed only after
        the learning rate had been stabilized.} Raising $\alpha$ from
        0.50 to 0.90 accelerated both activation families, but its
        effect was primarily on epochs-to-convergence rather than on the
        final score.

  \item \textbf{A 12-node hidden layer was more reliable than a 5-node
        hidden layer.} The smaller Tanh network remained acceptable, but
        the smaller Leaky ReLU network failed badly.  Hidden-layer width
        should therefore be treated as a capacity constraint, not just a
        speed knob.

  \item \textbf{Leaky ReLU delivered the best peak accuracy, while Tanh
        provided the smoother efficiency--accuracy tradeoff.}
        \texttt{networkB\_improv2} is the best model when prediction
        quality is the sole criterion, whereas
        \texttt{networkA\_improv2\_fast} is the strongest Tanh choice
        when faster convergence is also valued.

  \item \textbf{SMOTE effectively neutralized class imbalance,} enabling
        all eight classes to contribute proportionally to the gradient
        signal.  Future work should apply SMOTE only to the training
        fold, after the train/validation split, to remove leakage from
        the validation set.
\end{enumerate}

% -----------------------------------------------------------------------
% \section{References}
% -----------------------------------------------------------------------

\begin{thebibliography}{9}

\bibitem{rumelhart1986}
D.~E. Rumelhart, G.~E. Hinton, and R.~J. Williams,
``Learning representations by back-propagating errors,''
\textit{Nature}, vol.~323, pp.~533--536, 1986.

\bibitem{chawla2002}
N.~V. Chawla, K.~W. Bowyer, L.~O. Hall, and W.~P. Kegelmeyer,
``SMOTE: Synthetic minority over-sampling technique,''
\textit{Journal of Artificial Intelligence Research},
vol.~16, pp.~321--357, 2002.

\bibitem{brownlee2021}
J.~Brownlee,
``SMOTE for imbalanced classification with Python,''
\textit{Machine Learning Mastery}, 2021.
[Online]. Available:
\url{https://machinelearningmastery.com/smote-oversampling-for-imbalanced-classification/}

\bibitem{lecun1998}
Y.~LeCun, L.~Bottou, G.~B. Orr, and K.-R. M\"{u}ller,
``Efficient backprop,''
in \textit{Neural Networks: Tricks of the Trade},
G.~Orr and K.-R. M\"{u}ller, Eds.
Springer, Berlin, Heidelberg, 1998, pp.~9--50.

\end{thebibliography}

\end{document}
